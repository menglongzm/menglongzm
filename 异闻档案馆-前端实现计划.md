# 「异闻档案馆」前端实现计划

> 项目代号：`anomaly-archive`  
> 定位：一个偏好玩、可本地跑通的**个人桌面式工作台**  
> 目标：练熟「多 Tab 容器 + 筛选表格 + 弹窗操作 + 待办双 Tab + Mock 数据层」  
> 范围：**只做前端**，不接真实后端  
> 工期建议：约 7～8 天（每天 2～3 小时）

---

## 1. 产品故事（为什么有意思）

你不是在做冷冰冰的后台 CRUD，而是在搭建一座 **「城市异闻档案馆」** 的值班控制台。

你扮演一名档案官，日常工作包括：

1. 接收市民上报的奇异事件  
2. 按「现象类型」给案件分配调查员  
3. 汇总所有异闻档案  
4. 处理失效线索与异常案件  
5. 下发外勤调查任务  
6. 跟踪调查进度  
7. 管理证物与录音草稿  
8. 查看本周调查进度总表  
9. 对结案报告做「待我复核 / 全部」审批

界面风格类似后台系统，但文案、状态、字段全部换成异闻主题，做起来更有沉浸感。

### 1.1 明确不做

- 真实登录服务 / 数据库 / 云存储  
- 复杂权限树 / 工作流引擎  
- 移动端优先适配  
- 华丽 3D / 游戏引擎  
- 真实后端 API

### 1.2 做完你将得到

- 可本地运行的 Vite + Vue3 项目  
- 1 个系统壳（顶栏 + 侧栏 + 内容区）  
- 2 个核心业务页（多 Tab 工作台 + 双 Tab 复核台）  
- 一套 mock 数据与 localStorage 持久化  
- 可对照本文逐步编码复现

---

## 2. 技术栈（固定，不要中途换）

| 层级 | 选型 | 用途 |
|---|---|---|
| 构建 | Vite 5 | 开发与打包 |
| 框架 | Vue 3.4+ | 页面与组件 |
| 写法 | Composition API + `<script setup>` | 现代 Vue 写法 |
| 路由 | Vue Router 4 | 页面切换 |
| 状态 | Pinia | 用户、权限、待办角标、主题 |
| UI | Element Plus | Tabs / Form / Table / Dialog / Drawer / DatePicker / Tag / Message |
| 高级表格（可选） | vxe-table | 仅「进度总表」页练列配置与单元格编辑 |
| 请求层 | axios | 保持「页面 → api 函数 → 数据」结构 |
| 工具 | dayjs、lodash-es | 日期、深拷贝、防抖 |
| 样式 | SCSS + CSS 变量 | 暗色档案馆主题 |
| 持久化 | localStorage | mock 数据、列配置、草稿 |
| 语言 | JavaScript | 降低门槛，暂不上 TypeScript |

安装命令：

```bash
npm create vite@latest anomaly-archive -- --template vue
cd anomaly-archive
npm i
npm i element-plus vue-router pinia axios dayjs lodash-es
npm i -D sass
```

可选（阶段 5 后再装）：

```bash
npm i vxe-table xe-utils
```

---

## 3. 信息架构

### 3.1 路由

```text
/login                         假登录（选身份：档案官 / 主审官）
/app
  /dashboard                   值班概览
  /archive-desk                【核心页 A】异闻工作台（8 Tab）
  /review-board                【核心页 B】结案复核台（2 Tab）
  /settings                    设置（主题、重置数据、切换身份）
```

### 3.2 核心页 A：`/archive-desk` 异闻工作台（8 Tab）

| key | Tab 名称 | 组件 | 职责 |
|---|---|---|---|
| `1` | 按现象分配 | `TopicAllocate.vue` | 从现象类型维度看待分配案件 |
| `2` | 调查提交 | `CaseSubmit.vue` | 勾选已分配案件，提交调查计划 |
| `3` | 异闻汇总 | `CaseCollect.vue` | 全量档案查询、结案/搁置、日志 |
| `4` | 异常处理 | `CaseException.vue` | 超期、失效、信息矛盾案件 |
| `5` | 外勤下发 | `FieldDispatch.vue` | 把案件推到本周外勤任务 |
| `6` | 调查进度 | `FieldProgress.vue` | 跟踪外勤完成度 |
| `7` | 证物草稿 | `EvidenceDrafts.vue` | 证物描述/录音笔记本地草稿 |
| `8` | 进度总表 | `ProgressBoard/` | 周度调查量、可编辑本周目标 |

默认激活：`3` 异闻汇总。

### 3.3 核心页 B：`/review-board` 结案复核（2 Tab）

| Tab | searchType | 含义 |
|---|---:|---|
| 待我复核 | 1 | 当前身份需要处理的结案单 |
| 全部 | 2 | 所有结案复核单 |

两个 Tab **共用同一个列表组件** `ToReview.vue`。

---

## 4. 目录结构（按此创建）

```text
anomaly-archive/
├─ package.json
├─ vite.config.js
├─ index.html
├─ public/
│  └─ favicon.svg
├─ mock/
│  ├─ db.js                 # 初始全量数据
│  ├─ cases.js
│  ├─ missions.js
│  ├─ reviews.js
│  └─ index.js
├─ src/
│  ├─ main.js
│  ├─ App.vue
│  ├─ styles/
│  │  ├─ variables.scss
│  │  ├─ reset.scss
│  │  └─ common.scss
│  ├─ router/index.js
│  ├─ stores/
│  │  ├─ user.js
│  │  ├─ app.js
│  │  └─ badge.js
│  ├─ api/
│  │  ├─ http.js
│  │  ├─ cases.js
│  │  ├─ missions.js
│  │  ├─ reviews.js
│  │  └─ common.js
│  ├─ utils/
│  │  ├─ storage.js
│  │  ├─ date.js
│  │  ├─ status.js
│  │  └─ eventBus.js
│  ├─ components/
│  │  ├─ layout/
│  │  │  ├─ AppLayout.vue
│  │  │  ├─ AppHeader.vue
│  │  │  ├─ AppSidebar.vue
│  │  │  └─ AppBreadcrumb.vue
│  │  ├─ common/
│  │  │  ├─ SearchBar.vue
│  │  │  ├─ PageCard.vue
│  │  │  ├─ EmptyState.vue
│  │  │  ├─ StatusTag.vue
│  │  │  ├─ ColumnSortDialog.vue
│  │  │  └─ UploadMock.vue
│  │  └─ business/
│  │     ├─ CaseDetailDrawer.vue
│  │     ├─ MissionEditDialog.vue
│  │     └─ ReviewDetailDialog.vue
│  └─ views/
│     ├─ login/index.vue
│     ├─ dashboard/index.vue
│     ├─ archive-desk/
│     │  ├─ index.vue
│     │  ├─ tabs/
│     │  │  ├─ TopicAllocate.vue
│     │  │  ├─ CaseSubmit.vue
│     │  │  ├─ CaseCollect.vue
│     │  │  ├─ CaseException.vue
│     │  │  ├─ FieldDispatch.vue
│     │  │  ├─ FieldProgress.vue
│     │  │  ├─ EvidenceDrafts.vue
│     │  │  └─ ProgressBoard/
│     │  │     ├─ index.vue
│     │  │     ├─ FilterBar.vue
│     │  │     └─ ImportDialog.vue
│     │  └─ components/
│     │     ├─ CaseActionDialog.vue
│     │     └─ CaseLogDialog.vue
│     ├─ review-board/
│     │  ├─ index.vue
│     │  └─ toReview.vue
│     └─ settings/index.vue
```

---

## 5. 组件关系（实现时严格按此分层）

### 5.1 应用壳

```text
App.vue
 └─ RouterView
     ├─ Login
     └─ AppLayout
         ├─ AppHeader（站名、待办角标、用户菜单）
         ├─ AppSidebar（概览 / 异闻工作台 / 结案复核 / 设置）
         └─ Content RouterView
             ├─ Dashboard
             ├─ ArchiveDesk（8 Tab）
             ├─ ReviewBoard（2 Tab）
             └─ Settings
```

### 5.2 工作台

```text
views/archive-desk/index.vue
 ├─ el-tabs（只负责标题 + activeIndex）
 ├─ v-if / v-else-if 渲染当前业务子页
 │   ├─ TopicAllocate
 │   ├─ CaseSubmit
 │   ├─ CaseCollect
 │   │    ├─ CaseDetailDrawer
 │   │    ├─ CaseActionDialog
 │   │    └─ CaseLogDialog
 │   ├─ CaseException
 │   ├─ FieldDispatch
 │   ├─ FieldProgress
 │   ├─ EvidenceDrafts
 │   └─ ProgressBoard/index
 │        ├─ FilterBar  --emit search-handler--> 父组件
 │        ├─ el-table / vxe-table
 │        └─ ImportDialog
 └─（可选）容器级日志弹窗
```

### 5.3 复核台

```text
views/review-board/index.vue
 ├─ Tab 待我复核 → ToReview(:searchType="1")
 ├─ Tab 全部     → ToReview(:searchType="2")
 └─ ReviewDetailDialog
```

### 5.4 通信约定（固定）

| 场景 | 方式 |
|---|---|
| 父 → 子配置 | `props` |
| 子 → 父查询 | `emit('search-handler', form)` |
| 打开详情 | 父组件持有 `currentRow + visible` |
| 跨页刷新角标 | Pinia `badgeStore.refresh()` |
| 列显隐/顺序 | localStorage |
| 业务列表数据 | **只放页面本地**，不进 Pinia |

---

## 6. 领域模型（Mock 数据结构）

### 6.1 异闻案件 `Case`

```js
{
  id: 'AN-202607-001',
  title: '凌晨三点的公交站重复广播',
  phenomenon: '时间错位',        // 现象类型
  district: '雾港区',           // 城区（对应原“厂区/区域”）
  reporter: '匿名市民',
  assignee: '林深',             // 调查员
  status: 20,                   // 见状态枚举
  riskLevel: 'high',            // high/medium/low
  clueScore: 72,                // 线索完整度 0-100
  evidenceCount: 3,
  createdAt: '2026-07-01 21:13:00',
  updatedAt: '2026-07-14 10:20:00',
  deadline: '2026-07-20',
  summary: '同一则到站语音在空站循环出现……',
  changeType: 'none'            // none/delay/close/transfer
}
```

### 6.2 状态枚举 `utils/status.js`

```js
export const CASE_STATUS = {
  10: '待受理',
  20: '调查中',
  30: '外勤中',
  40: '待复核',
  50: '已结案',
  60: '已搁置',
  70: '已失效'
}

export const MISSION_STATUS = {
  1: '已下发',
  5: '进行中',
  11: '已完成',
  13: '已撤销'
}

export const REVIEW_STATUS = {
  pending: '复核中',
  rejected: '已驳回',
  done: '已通过'
}

export const PHENOMENON_OPTIONS = [
  '时间错位',
  '记忆污染',
  '空间折叠',
  '失踪回声',
  '非自然生物',
  '器物异常'
]

export const DISTRICT_OPTIONS = [
  '雾港区', '旧城区', '大学城', '工业岛', '夜市环线'
]
```

### 6.3 外勤任务 `Mission`

```js
{
  id: 'MS-1001',
  caseId: 'AN-202607-001',
  caseTitle: '凌晨三点的公交站重复广播',
  phenomenon: '时间错位',
  district: '雾港区',
  weekStart: '2026-07-14',
  targetClues: 8,               // 本周目标线索数
  doneClues: 3,
  progress: 0.375,
  taskStatus: 5,
  pushBy: '档案官·阿宁',
  assignee: '林深'
}
```

### 6.4 结案复核单 `Review`

```js
{
  id: 'RV-3001',
  type: 'case-close',           // case-close / weekly-report / risk-upgrade
  title: '结案复核：凌晨三点的公交站重复广播',
  caseTitle: '凌晨三点的公交站重复广播',
  phenomenon: '时间错位',
  district: '雾港区',
  currentNode: 'archivist',     // archivist / chief / done
  currentHandlers: ['archivist'],
  leaders: ['chief'],
  planClues: 8,
  actualClues: 6,
  status: 'pending',
  creator: 'archivist',
  nextHandler: 'chief',
  createdAt: '2026-07-14 18:00:00',
  comment: ''
}
```

### 6.5 假用户

```js
// 档案官
{
  id: 'archivist',
  name: '阿宁',
  account: 'archivist',
  role: 'archivist',
  permissions: [
    'case_close',
    'case_shelve',
    'mission_edit',
    'mission_column_setting',
    'review_handle'
  ]
}

// 主审官
{
  id: 'chief',
  name: '沈校',
  account: 'chief',
  role: 'chief',
  permissions: [
    'review_handle',
    'mission_column_setting'
  ]
}
```

---

## 7. 视觉主题（档案馆气质）

| 项 | 取值 |
|---|---|
| 主色 | 雾紫 `#6C5CE7` |
| 辅色 | 青磷 `#00CEC9` |
| 危险 | `#FF6B6B` |
| 警告 | `#FDCB6E` |
| 成功 | `#00B894` |
| 背景 | 深灰蓝 `#0F1221`（可选暗色）或浅灰 `#F5F6FA`（默认先做浅色） |
| 卡片 | 白底 / 圆角 10 / 轻阴影 |
| 字体 | 标题略神秘，正文保持系统可读字体 |
| 状态标签 | 用 `StatusTag` 统一颜色映射 |

文案语气：

- 查询 → 「检索档案」
- 重置 → 「清空条件」
- 详情 → 「打开档案」
- 提交 → 「封存并提交」
- 空状态 → 「今夜暂无新的异闻」

---

## 8. 页面规格

### 8.1 AppLayout

**Header**

- 左：Logo + 「异闻档案馆」
- 中：可选全局搜索（mock）
- 右：待复核角标、用户名、切换身份、退出

**Sidebar**

```text
值班概览
异闻工作台
结案复核
系统设置
```

### 8.2 Dashboard（有趣但不复杂）

展示 4 张统计卡 + 1 个最近异闻列表：

- 调查中案件数  
- 本周外勤任务数  
- 待我复核  
- 高风险案件数  
- 最近 5 条案件动态

数据全部来自 mock 聚合，不另起后端。

### 8.3 工作台容器 `archive-desk/index.vue`

```vue
<script setup>
import { ref } from 'vue'
import TopicAllocate from './tabs/TopicAllocate.vue'
import CaseSubmit from './tabs/CaseSubmit.vue'
import CaseCollect from './tabs/CaseCollect.vue'
import CaseException from './tabs/CaseException.vue'
import FieldDispatch from './tabs/FieldDispatch.vue'
import FieldProgress from './tabs/FieldProgress.vue'
import EvidenceDrafts from './tabs/EvidenceDrafts.vue'
import ProgressBoard from './tabs/ProgressBoard/index.vue'

const activeIndex = ref('3')
</script>

<template>
  <page-card title="异闻工作台">
    <el-tabs v-model="activeIndex">
      <el-tab-pane label="按现象分配" name="1" />
      <el-tab-pane label="调查提交" name="2" />
      <el-tab-pane label="异闻汇总" name="3" />
      <el-tab-pane label="异常处理" name="4" />
      <el-tab-pane label="外勤下发" name="5" />
      <el-tab-pane label="调查进度" name="6" />
      <el-tab-pane label="证物草稿" name="7" />
      <el-tab-pane label="进度总表" name="8" />
    </el-tabs>

    <TopicAllocate v-if="activeIndex === '1'" />
    <CaseSubmit v-else-if="activeIndex === '2'" />
    <CaseCollect v-else-if="activeIndex === '3'" />
    <CaseException v-else-if="activeIndex === '4'" />
    <FieldDispatch v-else-if="activeIndex === '5'" />
    <FieldProgress v-else-if="activeIndex === '6'" />
    <EvidenceDrafts v-else-if="activeIndex === '7'" />
    <ProgressBoard v-else-if="activeIndex === '8'" />
  </page-card>
</template>
```

规则：

1. Tab 标题与业务组件分离  
2. 用 `v-if` 切换，离开即销毁，回来重新 `created/mounted`  
3. 容器几乎不写业务逻辑

---

### 8.4 Tab3 异闻汇总（第一优先，最完整）

**查询字段**

- 档案编号  
- 标题关键词  
- 现象类型  
- 城区  
- 调查员  
- 案件状态  
- 风险等级  
- 上报时间范围  

**按钮**

- 检索档案 / 清空条件 / 收起  
- 标记结案（权限 `case_close`）  
- 搁置档案（权限 `case_shelve`）  
- 操作日志  

**表格列**

档案编号、标题、现象、城区、调查员、状态、风险、线索完整度、证物数、更新时间、操作

**主流程**

```text
进入页 → 加载字典 + getCaseList
点检索 → 校验：档案编号 或 时间范围 至少一个
点打开档案 → CaseDetailDrawer
点结案/搁置 → CaseActionDialog → apiUpdateCaseStatus → 刷新
点日志 → CaseLogDialog
```

**查询校验伪代码**

```js
function validateQuery(form) {
  if (!form.id && !(form.dateRange && form.dateRange.length === 2)) {
    ElMessage.warning('请填写档案编号或选择上报时间范围')
    return false
  }
  return true
}
```

---

### 8.5 Tab8 进度总表（第二优先）

**FilterBar 字段**

- 周起始日（默认本周一，展示 `MM月DD日`）  
- 城区  
- 现象类型  
- 案件标题  
- 档案编号  
- 任务状态  

**工具按钮**

- 检索 / 清空  
- 编辑（`mission_edit`）  
- 列设置（`mission_column_setting`）  
- 导入（mock）  
- 导出 CSV  

**表格列**

任务状态、城区、下发人、调查员、档案编号、标题、现象、目标线索、已完成线索、剩余线索、本周目标、周进度、周起始日

**父子通信**

```text
FilterBar 修改条件
  → emit('search-handler', query)
  → ProgressBoard 合并 searchForm
  → page = 1
  → fetchList()
```

**可编辑字段**

仅 `targetClues`：失焦 → `apiSaveWeeklyTarget` → 成功后重查

**第一版表格**

先用 `el-table` + 插槽编辑；跑通后再可选换 `vxe-table`。

---

### 8.6 其余 6 个 Tab（最小闭环即可）

| Tab | 最小功能 |
|---|---|
| 按现象分配 | 左：现象列表；右：该现象下待分配案件；按钮「指派调查员」 |
| 调查提交 | 勾选已分配案件 →「提交本周调查计划」生成 Mission |
| 异常处理 | 只显示超期/失效/矛盾案件；支持恢复调查或确认失效 |
| 外勤下发 | 可下发列表 +「下发外勤」改 taskStatus |
| 调查进度 | 进度条列表，可改 doneClues |
| 证物草稿 | 左列表右编辑器，localStorage 保存文本草稿 |

统一模板：

```text
查询区 → 列表 → 1 个主操作 → mock api → 刷新
```

---

### 8.7 结案复核台

**`review-board/index.vue`**

- 两个 Tab 都渲染 `ToReview`  
- 详情弹窗放容器层  

**`toReview.vue` props**

```js
defineProps({
  searchType: { type: Number, required: true } // 1 待我 / 2 全部
})
```

**查询字段**

- 当前节点  
- 城区  
- 案件标题  
- 复核类型  

**表格列**

序号、复核类型、当前节点、当前处理人、案件标题、城区、计划线索、实际线索、发起人、下一节点、发起时间、状态、操作

**操作列规则**

```text
currentHandlers 包含当前用户 → 显示「处理」
否则 leaders 包含当前用户 → 显示「催办」
否则 → 显示「查看」
```

**详情弹窗**

- 查看：只读  
- 处理：填写意见，通过 / 驳回  
- 催办：提示已通知下一节点  

**成功后**

```text
关弹窗 → 列表刷新 → badgeStore.refresh()
```

---

## 9. API 层（无后端，保留后端思维）

### 9.1 `api/http.js`

- 创建 axios 实例（baseURL 可写 `/api`）  
- 请求拦截：附加 `Authorization: Bearer ${token}`  
- 响应拦截：约定 `{ code, data, msg }`，`code !== 0` 报错  
- 实际阶段 1～7：api 函数内部直接走 mock，不必真发网络  

### 9.2 Mock 方式（推荐）

```js
// utils/mockRequest.js
export function mockRequest(executor, delay = 280) {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      try {
        resolve({ code: 0, data: executor(), msg: 'ok' })
      } catch (e) {
        reject({ code: 1, data: null, msg: e.message || 'error' })
      }
    }, delay)
  })
}
```

```js
// api/cases.js
import { mockRequest } from '@/utils/mockRequest'
import { loadCases, saveCases } from '@/utils/storage'

export function getCaseList(params) {
  return mockRequest(() => {
    let list = loadCases()
    // 过滤 + 分页
    return { rows: pageList, total }
  })
}
```

### 9.3 必备接口清单

**cases**

- `getCaseList`  
- `getCaseDetail`  
- `updateCaseStatus`  
- `assignInvestigator`  
- `getCaseLogs`  
- `getPhenomenonOptions`  
- `getDistrictOptions`  

**missions**

- `getMissionList`  
- `saveWeeklyTarget`  
- `pushMissions`  
- `importMissions`  
- `exportMissions`（前端导 CSV）  
- `getColumnSort` / `saveColumnSort`  

**reviews**

- `getReviewPage`  
- `getReviewDetail`  
- `submitReview`  
- `rejectReview`  
- `followUpReview`  
- `getReviewCount`  

---

## 10. Pinia 设计

### `stores/user.js`

```js
// state: token, userInfo, permissions
// actions: login(roleKey), logout(), hasPermission(code)
```

### `stores/badge.js`

```js
// state: reviewCount
// actions: refresh() → getReviewCount()
```

### `stores/app.js`

```js
// state: collapsed, themeMode
```

列表、分页、筛选表单：**全部留在页面组件**。

---

## 11. 初始 Mock 数据建议

在 `mock/db.js` 预置：

- 24 条案件（覆盖 6 种现象、5 个城区、多种状态）  
- 14 条外勤任务（含本周、上周）  
- 10 条复核单（待我 / 全部都有数据）  
- 每条案件 1～3 条操作日志  

给案件起有画面感的标题，例如：

- 《夜班便利店的第二位店员》  
- 《只会在雨天出现的旧电梯层》  
- 《收件人是十年前自己的快递》  
- 《相册里多出的陌生合影》  
- 《凌晨三点的公交站重复广播》  

---

## 12. 分阶段实施计划（照着做）

### 阶段 0：脚手架（0.5 天）

1. 创建 Vite Vue 项目  
2. 安装依赖  
3. 配置 `@ → src`  
4. 接入 Element Plus / Router / Pinia  
5. 跑通 `AppLayout + 4 个空页面`  

**验收：** 侧栏能切换页面。

---

### 阶段 1：设计系统与公共组件（0.5 天）

实现：

- `variables.scss` / `common.scss`  
- `PageCard` / `StatusTag` / `EmptyState` / `SearchBar`  
- `storage.js` / `status.js` / `date.js` / `mockRequest.js`  

**验收：** Dashboard 能显示主题色标签与空状态。

---

### 阶段 2：假登录与权限（0.5 天）

1. 登录页两张身份卡：档案官 / 主审官  
2. 写入 token + userInfo  
3. 路由守卫  
4. `hasPermission(code)`  

**验收：** 不同身份按钮可见性不同。

---

### 阶段 3：Mock 数据中心（0.5 天）

1. 完成 `mock/db.js`  
2. `loadXxx / saveXxx` 读写 localStorage  
3. 设置页「重置演示数据」  

**验收：** 刷新后数据还在；重置后恢复初始。

---

### 阶段 4：工作台容器 + 异闻汇总（1.5 天）★核心

1. `archive-desk/index.vue` 8 Tab 骨架  
2. `CaseCollect.vue` 全流程  
3. 详情抽屉、状态变更弹窗、日志弹窗  
4. 查询校验  

**验收：**

- 能检索  
- 能结案/搁置  
- 能看日志  
- 切走再切回会重新请求  

---

### 阶段 5：进度总表（1 天）

1. `FilterBar` + `search-handler`  
2. 列表与字段格式化  
3. 编辑本周目标  
4. 列显隐（checkbox + localStorage）  
5. CSV 导出；导入可用 JSON 粘贴模拟  

**验收：** 改目标后表格与存储同步变化。

---

### 阶段 6：其余 6 Tab 最小闭环（1～1.5 天）

顺序建议：

1. 外勤下发  
2. 调查提交  
3. 按现象分配  
4. 调查进度  
5. 异常处理  
6. 证物草稿  

**验收：** 8 个 Tab 都能打开且主按钮可用。

---

### 阶段 7：结案复核双 Tab（1 天）

1. `review-board/index.vue` + `toReview.vue`  
2. `searchType` 数据范围  
3. 处理 / 催办 / 查看  
4. 通过驳回  
5. 角标联动  

**验收：**

- 档案官能处理一部分  
- 主审官能处理另一部分  
- 处理后角标变化  

---

### 阶段 8：打磨（0.5 天）

1. 统一 loading / 空态 / 错误提示  
2. Dashboard 真实聚合数字  
3. README：启动方式、演示账号、目录说明  
4. 按第 14 节清单自检  

---

## 13. 关键代码骨架

### 13.1 路由守卫

```js
router.beforeEach((to, from, next) => {
  const user = useUserStore()
  if (to.path !== '/login' && !user.token) return next('/login')
  if (to.path === '/login' && user.token) return next('/app/dashboard')
  next()
})
```

### 13.2 筛选子组件

```js
// FilterBar.vue
const emit = defineEmits(['search-handler', 'reset'])
const form = reactive({
  weekStart: '',
  district: '',
  phenomenon: '',
  taskStatus: ''
})

function onSearch() {
  emit('search-handler', { ...form })
}
```

```js
// ProgressBoard/index.vue
function searchHandler(payload) {
  Object.assign(searchForm, payload)
  page.value = 1
  getList()
}
```

### 13.3 复核操作列

```js
function resolveAction(row, account) {
  if (row.currentHandlers?.includes(account)) return 'handle'
  if (row.leaders?.includes(account)) return 'urge'
  return 'view'
}
```

### 13.4 权限按钮

```vue
<el-button
  v-if="userStore.hasPermission('case_close')"
  type="primary"
  @click="openClose"
>
  标记结案
</el-button>
```

---

## 14. 完成标准

### 功能

- [ ] 两种身份可登录切换  
- [ ] 工作台 8 Tab 可切换  
- [ ] 异闻汇总：检索 / 清空 / 详情 / 状态变更 / 日志  
- [ ] 进度总表：筛选 / 编辑目标 / 列显隐 / 导出  
- [ ] 复核：待我与全部数据不同  
- [ ] 处理后列表与角标更新  
- [ ] 刷新后数据保留  
- [ ] 设置页可重置数据  

### 结构

- [ ] 页面不直接散落操作 localStorage 细节（走 api/storage）  
- [ ] 容器组件不堆表格业务  
- [ ] 查询参数流清晰  
- [ ] 弹窗归属明确，无循环依赖  

### 学习自检（要能口述）

1. 为什么 Tab 内容用 `v-if`？  
2. 为什么「待我复核/全部」共用一个列表组件？  
3. `searchType` 和按钮权限有什么区别？  
4. 子筛选栏为什么要 `emit`？  
5. 哪些状态进 Pinia，哪些留页面？  

---

## 15. 建议工期

| 阶段 | 时间 |
|---|---|
| 脚手架 + 布局 + 公共组件 | 1 天 |
| mock + 登录权限 | 1 天 |
| 工作台 + 异闻汇总 | 1.5 天 |
| 进度总表 | 1 天 |
| 其余 Tab 最小闭环 | 1.5 天 |
| 复核双 Tab + 角标 | 1 天 |
| 打磨与 README | 0.5 天 |
| **合计** | **约 7～8 天** |

---

## 16. 实施优先级原则

1. **先容器，后详情**  
2. **先一条主链路**：汇总检索 → 改状态 → 刷新  
3. **先 el-table，后 vxe-table**  
4. **先 mock api，后才考虑替换真后端**（保持 api 文件稳定）  
5. **每做完一个 Tab 就手动回归切换与刷新**

---

## 17. 启动后的第一条执行清单

1. 建项目并安装依赖  
2. 搭 `AppLayout` + 4 路由空页  
3. 实现 `mock/db.js` + `api/http.js` + `mockRequest`  
4. 做 `archive-desk/index.vue` + `CaseCollect.vue`  
5. 做 `ProgressBoard`  
6. 做 `review-board`  
7. 回填其余 Tab 与 Dashboard  
8. 写 README，按第 14 节打勾验收  

---

## 18. 演示账号（写进 README）

| 身份 | 账号 | 能力 |
|---|---|---|
| 档案官·阿宁 | archivist | 案件处理、外勤编辑、部分复核 |
| 主审官·沈校 | chief | 复核处理、列设置 |

登录页无需密码，点身份卡即进入。

---

## 19. 一句话主线

```text
市民异闻上报
  → 按现象分配调查员
  → 提交调查计划
  → 档案汇总与异常处理
  → 外勤下发与进度跟踪
  → 证物草稿沉淀
  → 周进度总表
  → 结案复核（待我 / 全部）
```

按本文件从阶段 0 做到阶段 8，即可独立复现一套完整、有趣、可演示的前端工作台。
