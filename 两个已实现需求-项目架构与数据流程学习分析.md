# 两个已实现需求：项目架构、接口与数据流程学习分析

> **文档定位**：面向刚接触项目的实习生，解释当前代码“已经怎样实现”，不是待实施技术方案。  
> **文档 ID**：IMPLEMENTATION-STUDY-20260712  
> **修订号**：Revision 1  
> **分析日期**：2026-07-12  
> **分析方式**：只读代码、设计文档和 Git 元数据；未修改业务代码、配置、测试及远程仓库，未运行构建、测试、数据库查询或远程接口。  
> **独立复核状态**：PASS（隔离只读复核；BLOCKER 0 / MAJOR 0 / MINOR 0 / UNVERIFIED 0）  
> **输入文档**：
> - `技术设计_备货进度推送模板优化.md`
> - `报价审批页新增「产品名称」筛选-技术设计方案V2.md`

---

## 1. 先用一句话理解两个需求

### 需求一：备货进度推送模板优化

系统定期计算冻品订单的备货进度。当订单进入 25%、50%、75% 或 100% 进度区间且当前节点没有推送记录时，系统给客户经理发送钉钉消息。

这次已经实现的优化是：在原消息后面增加三项重量信息，并将所有 KG 数值四舍五入为整数：

```text
(已加工XXKG；已发货XXKG；目前可发货XXKG)
```

它是一个**后端定时/外部调度触发的消息推送功能**，不是前端点击按钮后直接发送。

### 需求二：报价审批页增加“产品名称”筛选

用户在【订单】-【报价审批】的“待我审批”或“全部”页中：

1. 输入产品名称或产品编码的一部分；
2. 浏览器从已经加载的产品列表中筛出候选项；
3. 用户选择一个具体产品；
4. 页面显示产品名称，但提交给后端的是 `productCode`；
5. 后端用 `product_code = productCode` 精确查询包含该产品的审批单。

它是一个典型的**跨前端、产品查询服务、报价审批服务和数据库**的查询功能。

---

## 2. 阅读项目之前，先认识 4 个本地仓库

本工作区不是一个单体项目，而是多个独立 Git 仓库放在同一目录中。

| 仓库 | 技术形态 | 在两个需求中的职责 |
|---|---|---|
| `my-a-soa-front-slaughter-opms` | Vue 2 前端 SPA | 报价审批页面、产品下拉筛选、审批分页展示；也有备货进度页面，但不参与钉钉模板拼接 |
| `my-sc-slaughter-oms` | Spring Boot 多模块后端 | 备货任务、进度计算、消息模板、推送记录和钉钉发送 |
| `muyuan-slaughter-frozen-business` | Spring Boot 多模块后端 | 报价审批分页、权限条件、产品编码精确筛选和分页结果组装 |
| `Muyuan-Biz-slaughter-opms` | Spring Boot 多模块后端 | 为前端提供已有的产品列表接口，并代理调用外部产品中心 |

报价筛选还依赖一个当前工作区没有源码的服务：

```text
slaughter_product_center（产品中心）
```

因此，本地可以完整分析“调用方怎样请求产品中心”，但无法继续确认产品中心内部用了哪张表、什么 SQL、怎样建立索引。

### 2.1 当前 Git 代码基线

| 仓库 | 分支 | HEAD | 分析开始前工作区状态 |
|---|---|---|---|
| `my-sc-slaughter-oms` | `16977-20260711-feature_2.0` | `381a7af3a30e7051ac92c94238fa8d5ef53089d7` | `.idea/jarRepositories.xml` 已修改；`.codegraph/`、Manager 测试目录未跟踪 |
| `my-a-soa-front-slaughter-opms` | `XQ-#16441冻品订单报价审批页新增产品名称筛选-0709-zsl` | `c5b2909303cabd2fc0cf1f2351c2e1b68f5ea3ba` | clean |
| `muyuan-slaughter-frozen-business` | `16441-20260709-feature_2.0` | `f5235925a4d186366111ad8e057036ace68fe693` | `.gitignore`、`.idea/jarRepositories.xml` 已修改；`.codegraph/` 未跟踪 |
| `Muyuan-Biz-slaughter-opms` | `master` | `a26e74c309e8310cb7c464e9ea4f208fdcf65cd9` | `nul` 未跟踪 |

这些既有本地差异均未发现进入本次两项需求的运行调用链；本文结论仍以“当前实际文件内容”为准，而不只以 HEAD 提交内容为准。本次分析没有改动这些既有状态。

---

## 3. 后端多模块项目应该怎样理解

三个本地 Java 后端仓库虽然目录名很多，但可以先使用下面这个简化心智模型（其中两个需求分别会经过不同仓库）：

```text
浏览器或外部系统
    ↓
Controller / Service（HTTP 入口，接收参数）
    ↓
Manager（业务流程编排、权限和计算）
    ↓
Biz（数据访问的薄封装）
    ↓
Mapper + Mapper XML（SQL）
    ↓
数据库
```

常见模块职责如下：

| 模块后缀 | 通俗解释 |
|---|---|
| `*-api` | DTO、VO、实体等跨层数据结构 |
| `*-service` | Controller、Manager 和主要业务编排 |
| `*-dal` | Biz、Mapper、Mapper XML 和数据库对象 |
| `*-base-common` | 常量、枚举、公共工具、公共配置 |
| `*-job` | 定时任务或 XXL-Job Handler；并非所有定时行为都一定在这里 |
| `*-external-api/service` | 对外契约或外部系统适配 |
| `*-starter` | 聚合各模块，启动一个 Spring Boot 进程 |

这里的 Maven 子模块主要是**代码分层**，不应直接理解为“每个模块单独部署一个微服务”。通常由 `starter` 将多个模块装入同一个进程。

前端则可以简化为：

```text
Vue 页面组件
    ↓
src/api 中的 Axios 封装
    ↓
/api/{服务名}/{Controller路径}
    ↓
网关转发至对应后端服务
```

Axios 请求拦截器会增加应用标识、Basic Authorization 和当前用户 token，证据见 `my-a-soa-front-slaughter-opms/src/router/axios.js:37`。

> 注意：本地能看到网关能力和服务名约定，但没有生产环境完整动态路由配置。因此，`/api` 和服务名前缀具体在哪一层被剥离，不能只靠当前代码完全还原。

---

# 第一部分：备货进度推送模板优化

## 4. 这个需求涉及哪些模块

主要实现全部在 `my-sc-slaughter-oms`：

| 模块 | 作用 | 关键代码 |
|---|---|---|
| `order-service` | HTTP 入口、MQ 消费、计算进度、拼接消息 | `MyFrozenOrderStockPreparationTaskService`、`MyFrozenOrderStockPreparationTaskManager`、`MyFrozenOrderStockPreparationTaskListener` |
| `order-dal` | 备货主表、日度表、进度快照表、推送记录表、订单表查询 | 各 Biz、Mapper、Mapper XML、DO、VO |
| `common-dal` | 给钉钉 DTO 设置 appId/unionId，调用钉钉服务 | `MySendDingDingMessageBiz` |
| `base-common` | MQ topic、外部接口路径、常量 | `QueueConstants`、`OpmsServiceInterfaceConstant` 等 |
| `starter` | 将上述模块装入可执行应用 | `SlaughterOMSApplication` |

前端备货页面会展示 `cumulativeDailyWorkT`、`deliverGoods`、`shippedWeight`，但本次钉钉模板不是由页面生成。因此应区分：

- 页面只是查看备货数据；
- 后端 Manager 才负责计算里程碑和拼接钉钉消息。

---

## 5. 备货数据是怎样进入系统的

要理解推送消息，先要知道 `taskVO` 从哪里来。

### 5.1 订单备货主数据：MQ 写入主表

当前工作区可以看到 MQ 消费端：

```text
上游系统发送备货任务 MQ（生产者源码不在当前工作区）
    ↓
RocketMQ topic: FROZEN_ORDER_STOCK_PREPARATION_TOPIC
    ↓
MyFrozenOrderStockPreparationTaskListener.consumeMessage()
    ↓
解析为 MyFrozenOrderStockPreparationTaskDO
    ↓
MyFrozenOrderStockPreparationTaskManager.addOrEditStockPreparationTask()
    ↓
按“订单号 + 产品编码”新增或更新
    ↓
my_frozen_order_stock_preparation_task
```

证据：

- MQ Consumer 注册：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/rocket/config/MyRocketMqServerConsumerOmsConfig.java:282`
- MQ 消息解析：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/listener/MyFrozenOrderStockPreparationTaskListener.java:33`
- 主表新增/更新：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/manager/order/beihuo/MyFrozenOrderStockPreparationTaskManager.java:140`

订单号、客户、厂区、产品、订单总量和已发货重量等信息最初随 MQ 对象进入备货主表，但要区分“新增”和“已有记录更新”：

- **新增记录**：直接以 MQ 解析出的 `MyFrozenOrderStockPreparationTaskDO` 为主体写入，因而会初始化消息中携带的主表字段；
- **已有记录**：当前更新分支只显式刷新 `taskStatus`、`productName`、备货起止时间、`orderQuantityKg`、`requiredPreparationKg`、占用数量/重量、已发货数量/重量、转冻结库数量/重量；
- 客户名称、客户编码、厂区名称/编码、订单号等没有出现在该已有记录更新分支中，因此不能说它们会被每一条后续 MQ 消息持续重写。

这意味着模板字段的新鲜度并不完全相同：例如 `shippedWeight` 会由已有记录分支刷新，而客户和厂区字段通常保留任务首次写入或其他路径留下的主表值。对应代码见 `MyFrozenOrderStockPreparationTaskManager.java:140-174`。

还要注意 MQ 失败边界：`consumeMessage()` 外层本来会在一般异常时返回 `RECONSUME_LATER`，但 `handleMessage()` 自己又捕获了业务处理异常且没有重新抛出。因此 `addOrEditStockPreparationTask()` 的异常可能无法传播到外层重试分支。最终 Broker ACK/重试仍取决于消费框架运行行为，当前未运行验证；不能简单理解为“所有落库异常都会自动重试”。证据见 `MyFrozenOrderStockPreparationTaskListener.java:33-96`。

### 5.2 每日实际加工量：外部接口写入日度表

系统还会从外部接口获取每天实际加工了多少：

```text
POST /stockPreparation/getDailyTaskWork
    ↓
Manager.getDailyTaskWork()
    ↓
调用外部 MDM 接口 GET_DAILY_TASK_WORK
    ↓
按“订单号 + 产品编码 + 日期”整理 productWeight
    ↓
写入 my_frozen_order_stock_preparation_daily_task.order_day_work
    ↓
重算主表累计加工量和可发货量
```

证据：

- HTTP 入口：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/service/order/beihuo/MyFrozenOrderStockPreparationTaskService.java:131`
- 外部数据拉取与转换：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/manager/order/beihuo/MyFrozenOrderStockPreparationTaskManager.java:1126`
- `productWeight` 写为 `orderDayWork`：同文件 `:1211`

### 5.3 主表累计字段怎样计算

`calculateCumulativeDailyWorkBeforeT3()` 会汇总日度表：

```text
T-1 加工总量 = Σ(order_day_work，备货日期 <= 今天-1)
T-3 加工总量 = Σ(order_day_work，备货日期 <= 今天-3)
分配时占用库存 = 订单总量 - 需备货量
主表 deliverGoods = 分配时占用库存 + T-3 加工总量
```

然后写回主表：

- `cumulativeDailyWorkBeforeT1 = T-1 加工总量`
- `cumulativeDailyWorkBeforeT3 = T-3 加工总量`
- `cumulativeDailyWorkT = T-1 加工总量`（在这条重算路径中）
- `deliverGoods = 分配时占用库存 + T-3 加工总量`

证据：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/manager/order/beihuo/MyFrozenOrderStockPreparationTaskManager.java:1703`，尤其是 `:1773-1801`。

这不是 `deliverGoods` 唯一的写入入口：调厂业务调整订单量级和需备货量时，也会按 `新订单量级 - 新需备货量 + cumulativeDailyWorkBeforeT3` 重算并写回该字段，证据见 `my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/manager/changeFactory/MyChangeFactoryQuantityManager.java:377-401`。因此，本文表格中的“主要来源”指常规定时累计链，不代表唯一写入者。

> 初学者容易混淆：字段注释称 `cumulativeDailyWorkT` 是“T 日总备货量”，但不同更新路径对它的计算时点并不完全相同。推送时读取的是主表快照，而不是直接拿本次 T-1 汇总的局部变量。

---

## 6. 推送从触发到钉钉的完整流程

### 6.1 实际可确认的触发入口

Spring MVC 入口为：

```http
POST /stockPreparation/calculateProgressAndPush
```

- 请求体：无
- 返回：`ObjectRestResponse.success()`
- 注解：`@IgnoreClientToken`、`@IgnoreUserToken`
- Controller：`MyFrozenOrderStockPreparationTaskService.calculateProgressAndPush()`

证据：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/service/order/beihuo/MyFrozenOrderStockPreparationTaskService.java:152`。

旧设计文档写“早上 9:00 定时任务”，但当前四个本地仓库中没有找到调用该方法的 `@Scheduled`、XXL-Job Handler 或前端代码。因此准确结论是：

- 本地代码证明存在一个可供调度系统调用的 HTTP 接口；
- 生产环境是否每天 9:00 调用、由哪个调度平台调用，当前工作区无法证明。

如果网关沿用同项目其他接口约定，外部路径可能带服务前缀，例如 `/api/muyuan-slaughter-opms/...`；但该触发接口没有前端调用证据，本文只把 Spring MVC 路径作为确定事实。

### 6.2 完整主链路

```text
外部调度器或人工 HTTP 调用
    ↓
POST /stockPreparation/calculateProgressAndPush
    ↓
MyFrozenOrderStockPreparationTaskService.calculateProgressAndPush()
    ↓
MyFrozenOrderStockPreparationTaskManager.calculateProgressAndPush()
    ├─ 查询近 3 天更新且状态为 1/5/11 的备货订单
    ├─ 从日度表分别汇总 T-1、T-3 加工量
    ├─ 查询/更新进度快照表 quantity
    ├─ 查询推送记录表，判断当前节点是否已经处理
    ├─ 从订单表批量取得客户经理工号
    ├─ 拼接消息并调用钉钉适配层
    └─ 插入推送记录
        ↓
MySendDingDingMessageBiz.sendFrozenOrderTextMessage()
    ├─ 设置生产/测试 appId
    ├─ 设置冻品订单消息助手 unionId
    ├─ 非生产环境增加“(测试环境)”前缀
    └─ DingtalkWindowMsgService.push(WindowTextDTO)
        ↓
外部钉钉服务（实现不在当前工作区）
```

核心方法：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/manager/order/beihuo/MyFrozenOrderStockPreparationTaskManager.java:1822`。

### 6.3 第一步：选择哪些订单参与计算

SQL 只选择：

- `update_time >= 三天前当天 00:00:00`
- `task_status IN (1, 5, 11)`

```sql
SELECT ...
FROM my_frozen_order_stock_preparation_task
WHERE update_time >= #{threeDaysAgo}
  AND task_status IN (1, 5, 11)
```

证据：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-dal/src/main/resources/mapper/order/beihuo/MyFrozenOrderStockPreparationTaskMapper.xml:365`。

### 6.4 第二步：计算进度

实际公式：

```text
分配时占用库存 = orderQuantityKg - requiredPreparationKg
T-3 可发货加工量 = 截止今天-3的 order_day_work 之和
进度 = (分配时占用库存 + T-3 可发货加工量)
       ÷ orderQuantityKg
       × 100%
```

计算规则：

- 先按 4 位小数除法，`HALF_UP`；
- 乘 100 后保留 2 位小数，`HALF_UP`；
- 超过 100 时钳制为 100；
- 订单总量为空或小于等于 0 时跳过；
- `requiredPreparationKg` 为空时按 0；
- 日度加工量为空时忽略，无记录时按 0。

核心代码证据：`MyFrozenOrderStockPreparationTaskManager.java:1913-1942`。

### 6.5 第三步：判断里程碑

当前代码只判断“订单目前所在的那个区间”：

| 当前进度 | `currentMilestone` | 业务展示节点 |
|---|---:|---:|
| `<25%` | `null` | 不推送 |
| `[25%, 50%)` | `1` | 25% |
| `[50%, 75%)` | `2` | 50% |
| `[75%, 100%)` | `3` | 75% |
| `>=100%` | `4` | 100% |

数据库记录的是字符串 `"1"`、`"2"`、`"3"`、`"4"`，不是 `"25"`、`"50"`、`"75"`、`"100"`。

这会产生一个重要行为：如果某订单第一次计算就到了 80%，当前只处理 75% 节点，不会补发 25% 和 50% 节点。

证据：`MyFrozenOrderStockPreparationTaskManager.java:1983-2004`。

### 6.6 第四步：推送去重

去重依赖：

```text
my_frozen_order_stock_preparation_publish_record
```

系统在方法开始时批量查询有效记录，按 `main_id` 收集已经处理过的节点。若当前节点不在集合中，则执行发送并插入记录。

Mapper 证据：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-dal/src/main/resources/mapper/order/beihuo/MyFrozenOrderStockPreparationPublishRecordMapper.xml:22` 和 `:44`。

进度快照还会写入：

```text
my_frozen_order_stock_preparation_quantity
```

其中保存 T-1 总量、T-3 总量、进度和最高节点。但当前推送去重实际看的是 publish record 表，不是 quantity 表中的 `push_status`。

### 6.7 第五步：构建并发送消息

当前真实模板为：

```text
客户经理您好，您的订单：[订单号]，客户：[客户]，所属厂区：[厂区]，产品：[产品]，订单总量[整数]KG，已完成[进度%]，请及时跟进。
(已加工整数KG；已发货整数KG；目前可发货整数KG)
```

注意句号后有真实换行 `\n`。

字段映射：

| 消息文案 | Java 字段 | 主表列 | 主要来源 |
|---|---|---|---|
| 订单号 | `orderNo` | `order_no` | 上游备货 MQ |
| 客户 | `custName` | `cust_name` | 上游备货 MQ |
| 厂区 | `factoryName` | `factory_name` | 上游备货 MQ |
| 产品 | `productName` | `product_name` | 上游备货 MQ |
| 订单总量 | `orderQuantityKg` | `order_quantity_kg` | 上游备货 MQ |
| 已加工 | `cumulativeDailyWorkT` | `cumulative_daily_work_t` | 日加工数据累计后写回主表 |
| 已发货 | `shippedWeight` | `shipped_weight` | 上游备货 MQ 更新 |
| 目前可发货 | `deliverGoods` | `deliver_goods` | 占用库存 + T-3 加工累计 |
| 完成百分比 | `progressRate` | 本次计算局部变量 | 本次进度公式 |
| 接收人 | `customerManagerCode` | `my_order_info.customer_manager_code` | 按订单号批量查询 |

主表映射证据：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-dal/src/main/resources/mapper/order/beihuo/MyFrozenOrderStockPreparationTaskMapper.xml:5-50`。

取整逻辑：

```java
private BigDecimal roundToInteger(BigDecimal value) {
    if (value == null) return BigDecimal.ZERO;
    return value.setScale(0, RoundingMode.HALF_UP);
}
```

它应用于订单总量、已加工、已发货、目前可发货四个 KG 字段。`null` 显示为 `0`。百分比不经过这个方法。

消息和取整证据：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/manager/order/beihuo/MyFrozenOrderStockPreparationTaskManager.java:2036-2081`。

钉钉适配证据：`my-sc-slaughter-oms/my-sc-slaughter-oms-common-dal/src/main/java/com/muyuan/platform/slaughter/oms/common/dal/biz/MySendDingDingMessageBiz.java:40-59`。

---

## 7. 需求一的最小核心实现（教学简化版）

下面不是完整生产代码，而是帮助理解的最小模型：

```java
// 1. 计算进度
BigDecimal allocated = orderKg.subtract(requiredKg);
BigDecimal progress = allocated.add(t3ProcessedKg)
    .divide(orderKg, 4, RoundingMode.HALF_UP)
    .multiply(new BigDecimal("100"))
    .setScale(2, RoundingMode.HALF_UP);

// 2. 找到当前区间节点
Integer milestone = progress.compareTo(new BigDecimal("100")) >= 0 ? 4
    : progress.compareTo(new BigDecimal("75")) >= 0 ? 3
    : progress.compareTo(new BigDecimal("50")) >= 0 ? 2
    : progress.compareTo(new BigDecimal("25")) >= 0 ? 1
    : null;

// 3. 没处理过当前节点才发送
if (milestone != null && !published.contains(String.valueOf(milestone))) {
    String content = String.format(
        "...订单总量%sKG...\n(已加工%sKG；已发货%sKG；目前可发货%sKG)",
        round(orderKg), round(processedKg), round(shippedKg), round(deliverableKg)
    );
    dingTalk.push(content, customerManagerCode);
    publishRecord.insert(orderId, milestone);
}
```

可以把它记成：

```text
查订单 → 算进度 → 找区间 → 查去重 → 拼消息 → 发钉钉 → 记节点
```

---

## 8. 需求一的接口与数据表

### 8.1 直接相关 HTTP 接口

| 接口 | 方法 | 请求体 | 作用 | 响应 |
|---|---|---|---|---|
| `/stockPreparation/calculateProgressAndPush` | POST | 无 | 计算进度、推送并记录节点 | 通用成功响应 |
| `/stockPreparation/calculateCumulativeDailyWorkBeforeT3` | POST | 无 | 重算 T-1/T-3 累计和主表可发货量 | 通用成功响应 |
| `/stockPreparation/getDailyTaskWork` | POST | 无 | 从外部获取每日实际加工量并更新本地数据 | 通用成功响应 |
| `/stockPreparation/getStockPreparationTaskList` | POST | 查询 DTO | 供备货进度页面查询列表 | `TableResultResponse` |

旧设计文档提到用 `/frozenOrderWarnMessage/progressRiskWarn` 验证本需求，但该接口走的是另一套风险预警方法，不会调用 `calculateProgressAndPush()`，不能作为本功能的实际触发接口。

### 8.2 直接相关数据库表

| 表 | 作用 |
|---|---|
| `my_frozen_order_stock_preparation_task` | 备货任务主快照；消息中的大部分字段来自这里 |
| `my_frozen_order_stock_preparation_daily_task` | 每个任务、每个日期的实际加工量 `order_day_work` |
| `my_frozen_order_stock_preparation_quantity` | 本次计算得到的 T-1、T-3、进度和最高节点快照 |
| `my_frozen_order_stock_preparation_publish_record` | 已处理里程碑记录，用于去重 |
| `my_order_info` | 根据订单号取得客户经理工号 |

本地没有找到这些表的完整 DDL，所以不能确认真实索引、外键或唯一约束。

---

## 9. 需求一需要特别注意的当前实现语义

这些不是本次要求修复的问题，而是理解现状时不能忽略的行为。

### 9.1 “记录已推送”不等于“钉钉确认送达”

当前 `pushProgressMessage()`：

- 找不到客户经理时只写 warn；
- 捕获并吞掉发送异常；
- 忽略 `sendFrozenOrderTextMessage()` 的返回结果。

外层随后仍然插入 publish record。因此记录更接近“该节点已经尝试处理”，并不能严格证明消息成功送达。

组合证据：

- 调用后插记录：`MyFrozenOrderStockPreparationTaskManager.java:2002-2024`
- 发送方法内部吞异常：同文件 `:2036-2070`

### 9.2 发送和记录没有整体事务

当前未发现该流程的 `@Transactional`。例如：

- 钉钉发送成功、记录插入失败：下次可能重复发；
- 发送失败但记录插入成功：后续可能不再重试；
- 并发触发时，应用层“先查再插”也可能产生重复尝试。

本地没有表 DDL，无法确认数据库是否另有唯一键兜底。

### 9.3 模板字段和本次进度计算不是同一组变量

- `progressRate` 使用本次实时查询的 T-3 日度汇总计算；
- 模板中的 `cumulativeDailyWorkT` 和 `deliverGoods` 读取主表快照。

若主表累计任务没有在进度推送前及时运行，两者可能存在时间差。

### 9.4 “目前可发货”的语义要按当前字段理解

当前模板直接显示：

```text
deliverGoods = 分配时占用库存 + T-3 加工量
```

同仓库其他业务还有 `deliverGoods - shippedWeight` 作为剩余可用重量的计算。也就是说，“累计达到可发货条件的重量”和“扣除已发货后的当前余额”是两个不同概念。当前需求明确映射 `deliverGoods`，所以本文只描述已实现行为，不擅自改写业务口径。

---

# 第二部分：报价审批页“产品名称”筛选

## 10. 这个需求为什么实际涉及 3 个本地仓库

页面需要同时完成两件事：

1. 获取“有哪些产品可选”；
2. 按选中的产品查询“哪些审批单包含它”。

这两件事由不同服务完成：

```text
产品候选项：
前端 → Muyuan-Biz-slaughter-opms → 外部产品中心

审批单筛选：
前端 → muyuan-slaughter-frozen-business → 报价审批数据库表
```

因此不能只看 `toApproval.vue` 和一段 Mapper SQL。

---

## 11. 页面结构：“待我审批”和“全部”共用组件

父页面：

`my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/index.vue:1`

两个 tab 都创建 `toApproval.vue`，只通过 `searchType` 区分：

| tab | `searchType` | 当前含义 |
|---|---:|---|
| 待我审批 | 1 | 当前登录人正在处理的审批单 |
| 全部 | 2 | 按当前登录人的角色/历史处理范围查询；配置中的负责人可获得更大范围 |

两个 tab 都传 `orderType=1`，表示备货订单报价。

`v-if` 会在 tab 切换时销毁并重新创建组件。因此每次新建 tab 实例都会重新请求：

- 审批分页；
- 产品候选项。

它不是整个页面会话只获取一次的全局缓存。

当前子组件左侧标题也已经实现：

- `searchType === 1` 显示 `待我审批(n)`；
- 否则显示 `全部`。

筛选核心组件：`my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/toApproval.vue:1`。

---

## 12. 数据流 A：产品候选项从哪里来

### 12.1 浏览器端流程

组件挂载后：

```text
toApproval.vue mounted()
    ↓
getAllProductList()
    ↓
apiGetAllProductName({})
    ↓
POST /api/muyuan-slaughter-opms/myPreOrderInfo/getProductList
```

证据：

- 页面加载与转换：`my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/toApproval.vue:177-204`
- API 封装：`my-a-soa-front-slaughter-opms/src/api/slaughter-opms/frozenManagement/orderStockingProgressSchedule.js:31-38`

页面把后端对象转换成 Element UI 选项：

```javascript
{
  label: item.productName, // 用户看到的文字
  value: item.productCode  // v-model 实际保存的值
}
```

### 12.2 OPMS 代理服务流程

浏览器请求经过网关进入 `Muyuan-Biz-slaughter-opms`：

```text
POST /myPreOrderInfo/getProductList
    ↓
MyPreOrderService.getProductList(ProductDto)
    ↓
MyPreOrderManager.getProductList(productName, productCode)
    ↓
WebClientUtils.postByOneSys(...)
    ↓
POST ${opms.url.oms.prefix}
     /api/slaughter_product_center/common/myBaseDataRest/getProductByNameCode
```

代码证据：

- Controller：`Muyuan-Biz-slaughter-opms/muyuan-service/muyuan-slaughter-opms-external-service/src/main/java/org/muyuan/slaughter/opms/external/service/outside/order/MyPreOrderService.java:99-103`
- Manager：`Muyuan-Biz-slaughter-opms/muyuan-service/muyuan-slaughter-opms-oms-service/src/main/java/org/muyuan/slaughter/opms/oms/service/manager/order/beihuo/MyPreOrderManager.java:228-260`
- 下游路径常量：`Muyuan-Biz-slaughter-opms/muyuan-service-api/muyuan-slaughter-opms-api/src/main/java/org/muyuan/slaughter/opms/constant/UrlConstants.java:496-498`
- 服务间 token：`Muyuan-Biz-slaughter-opms/muyuan-service/muyuan-slaughter-opms/src/main/java/org/muyuan/slaughter/opms/utils/WebClientUtils.java:78-95`

浏览器发送 `{}`，所以 OPMS Controller 得到的 `ProductDto.productName` 和 `productCode` 都是 `null`。随后 Manager 构造的 Java `Map` 中包含 `productName`、`productCode` 两个键，当前 Java 值均为 `null`，并把该 Map 交给 WebClient。由于本次没有抓包或运行序列化测试，实际网络 JSON 是否显式保留两个 `null` 字段取决于运行时 Codec/ObjectMapper 配置，本文不把具体线格式写成确定事实。

调用方期望产品中心返回 `status=200` 和 `data` 列表，OPMS 将 `data` 转成 `List<MyBaseCustomerProduct>`，再以自己的 `R.data(...)` 返回前端。

### 12.3 接口契约示例

浏览器请求：

```http
POST /api/muyuan-slaughter-opms/myPreOrderInfo/getProductList
Content-Type: application/json
Authorization: ...

{}
```

前端实际依赖的响应形态：

```json
{
  "code": 200,
  "data": [
    {
      "productName": "冻猪肉丝H（10kg）",
      "productCode": "PRD001"
    }
  ]
}
```

真实 `MyBaseCustomerProduct` 还有分类、规格、状态等字段，页面只使用 `productName` 和 `productCode`。

### 12.4 这里的“remote”不是每次输入都请求后端

组件写了：

```html
<el-select filterable remote :remote-method="productProductData">
```

但 `productProductData()` 只过滤内存中的 `allProductList`：

```javascript
this.productOptions = this.allProductList.filter(
  item => item.label.includes(val) || item.value.includes(val)
)
```

所以准确流程是：

```text
组件挂载时请求一次候选列表，并把返回结果当作本组件的完整候选缓存
    ↓
用户每输入一个关键字
    ↓
浏览器本地 String.includes 过滤
    ↓
不再发送产品搜索 HTTP 请求
```

这里的“完整候选缓存”是前端使用方式，不代表本文已经证明产品中心的空条件契约必然返回数据库中的所有产品；产品中心源码和运行接口均未纳入当前工作区。

当前匹配特点：

- 名称或编码包含输入值即可；
- JavaScript `includes` 区分大小写；
- `trim()` 只判断是否为空，实际过滤仍使用原始 `val`；
- 没有节流、分页、结果条数限制、排序或去重；
- 空输入时恢复全量选项。

因此，它更准确地说是“具有远程下拉交互外观的前端本地过滤”。

### 12.5 本地另一个产品接口为什么不在真实链路中

`muyuan-slaughter-frozen-business` 中确实还有：

```text
/product/getProductSpec/getProductSpecInfo
→ MyMdProductSpecController
→ my_md_product_spec
```

但 `toApproval.vue` 没有调用这个接口。当前报价审批页真实使用的是 OPMS 代理到 `slaughter_product_center` 的接口。

所以旧设计文档中将 `MyMdProductSpecController` 当作可复用资产可以理解为设计阶段调查，但不能把它写成当前页面的实际数据链。

---

## 13. 数据流 B：选择产品后怎样查询审批列表

### 13.1 用户选择和清空

产品控件绑定：

```html
<el-select
  v-model="searchForm.productCode"
  clearable
  @change="search"
>
```

用户选择某项后：

1. 页面显示 `label=产品名称`；
2. `searchForm.productCode` 保存 `value=产品编码`；
3. `change` 触发 `search()`；
4. 页码重置为 1；
5. 发起审批分页请求。

清空后，`productCode` 变为空值，同样通过 `change` 重新查询；后端动态 SQL 不再添加产品条件。

证据：`my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/toApproval.vue:25-37` 和 `:185-232`。

### 13.2 前端请求组装

页面组装：

```javascript
const params = {
  ...this.searchForm,
  orderType: this.orderType,
  searchType: this.searchType,
  page: this.page.currentPage,
  limit: this.page.pageSize
}
```

接口：

```http
POST /api/muyuan-slaughter-frozen-business/apply/list/getApprovalPage
```

API 封装证据：`my-a-soa-front-slaughter-opms/src/api/slaughter-opms/frozenApproval/index.js:4-10`。

“待我审批”示例：

```json
{
  "currentBusinessNode": "",
  "customerAreaCode": "",
  "customerKey": "",
  "productCode": "PRD001",
  "orderType": 1,
  "searchType": 1,
  "page": 1,
  "limit": 100
}
```

“全部”只把 `searchType` 改为 `2`。

> Java DTO 将 `orderType` 定义为 `String`，而父组件当前通过 `:orderType="1"` 传入数字。这是已有类型不一致；JSON/Jackson 的具体转换由运行时配置负责，不是本筛选需求新增行为。

### 13.3 Controller：使用登录人工号覆盖请求值

后端入口：

```text
FrozenProductQuoteApplyInfoListController
@RequestMapping("/apply/list")
@PostMapping("/getApprovalPage")
```

Controller 会执行：

```java
dto.setJobNo(AuthUtil.getUserAccount());
return R.data(manager.getApprovalPage(dto));
```

因此客户端不能靠请求体伪造审批人工号；`jobNo` 最终来自当前登录用户。

证据：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-service/src/main/java/org/muyuan/slaughter/frozen/business/controller/quote/FrozenProductQuoteApplyInfoListController.java:45-55`。

DTO 字段见：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-api/src/main/java/org/muyuan/slaughter/frozen/business/dto/quote/MyFrozenProductQuoteApplyApprovalDTO.java:20-57`。

### 13.4 Manager：先 count，再 list，再补充产品信息

完整业务步骤：

```text
Manager.getApprovalPage(dto)
    ├─ jobNo 为空：返回空分页
    ├─ searchType=2 时判断当前人是否为配置中的运营负责人
    ├─ Mapper.getApprovalCount(dto)
    ├─ offset = (page - 1) × limit
    ├─ Mapper.getApprovalList(dto)
    ├─ 按本页 flowNo 批量查询全部有效产品详情
    ├─ 拼接 productNameList
    ├─ 汇总 productWeight（expectWeight 之和）
    ├─ 补充流程类型、最后审批节点、负责人列表
    └─ 返回 Page records + total
```

证据：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-service/src/main/java/org/muyuan/slaughter/frozen/business/manager/quote/MyFrozenProductQuoteApplyInfoListManger.java:245-317`。

Biz 只是薄封装：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-dal/src/main/java/org/muyuan/slaughter/frozen/business/biz/quote/MyFrozenProductQuoteApplyInfoBiz.java:55-65`。

### 13.5 Mapper：按产品编码精确判断“是否包含”

核心 SQL：

```xml
<if test="productCode != null and productCode != ''">
    and exists (
        select 1
        from my_frozen_product_quote_detail detail
        where detail.flow_no = t.flow_no
          and detail.product_code = #{productCode}
          and detail.status = 1
    )
</if>
```

通俗翻译：

> 对每一张审批主单，去产品明细表中检查：同一个 `flow_no` 下是否至少有一条有效明细，其产品编码等于用户选择的编码。只要存在，就保留这张审批单。

证据：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-dal/src/main/java/org/muyuan/slaughter/frozen/business/mapper/quote/MyFrozenProductQuoteApplyInfoMapper.xml:284-319`。

### 13.6 为什么使用 `EXISTS` 而不是直接 JOIN 产品明细

一张审批单可能包含多个产品。如果把产品明细直接 JOIN 到主查询：

```text
一张主单 × 多条产品明细 → 可能得到多行相同主单
```

`EXISTS` 只回答“有没有”，不会把明细行展开到主结果中，因此更符合筛选语义。

当前 count 仍使用 `COUNT(DISTINCT t.id)`，list 使用 `SELECT DISTINCT`，还因为审批记录表 `t2` 本身也可能有多条记录。

### 13.7 count 和 list 为什么一致

两个查询共同包含：

```xml
<include refid="getApprovalListSql" />
```

- count：`MyFrozenProductQuoteApplyInfoMapper.xml:320-328`
- list：同文件 `:366-413`

所以 `productCode`、权限、订单类型、节点、区域和客户条件会同时作用在总数和数据列表上。

### 13.8 筛选命中后，页面为什么还显示该单的全部产品

`EXISTS` 只用于决定“审批单是否命中”。主列表查完后，Manager 再按该页所有 `flowNo` 查询所有有效产品详情：

```sql
SELECT ...
FROM my_frozen_product_quote_detail
WHERE flow_no IN (...)
  AND status = 1
```

证据：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-dal/src/main/java/org/muyuan/slaughter/frozen/business/mapper/quote/MyFrozenProductQuoteDetailMapper.xml:116-126`。

所以：

- 选产品 A；
- 一张审批单包含 A、B、C；
- 这张单会命中；
- 页面“采购产品”仍显示 A、B、C，而不是只显示 A。

这是“按某产品筛选主单”与“展示主单完整详情”的正常区别。

### 13.9 返回前端

Controller 返回 `R.data(Page)`。页面实际使用：

```json
{
  "code": 200,
  "data": {
    "records": [
      {
        "applyNo": "A001",
        "flowNo": "F001",
        "customerName": "示例客户",
        "productNameList": "产品A,产品B",
        "productWeight": 200,
        "currentBusinessNodeName": "报价审批",
        "approvalStatus": "1",
        "orderType": "1"
      }
    ],
    "total": 1
  }
}
```

MyBatis Plus 的 `Page` 可能还序列化其他分页字段，但当前页面只读取：

- `res.data.data.records`
- `res.data.data.total`

证据：`toApproval.vue:225-232`。

---

## 14. 需求二的完整时序图

```text
用户打开“待我审批”tab
│
├─ created(): 请求审批分页（此时 productCode=""）
│    └─ frozen-business Controller
│         └─ Manager
│              ├─ count SQL
│              ├─ list SQL
│              ├─ 查询产品详情并补充展示字段
│              └─ R<Page> 返回页面
│
└─ mounted(): 请求全部产品候选项
     └─ muyuan-slaughter-opms Controller
          └─ MyPreOrderManager
               └─ 外部 slaughter_product_center
                    └─ 产品列表返回页面并缓存于组件实例

用户输入“肉丝”
└─ 浏览器本地 includes 过滤，不请求后端

用户选择“冻猪肉丝H（10kg）”
└─ v-model 保存 productCode="PRD001"
     └─ @change → search()
          ├─ page.currentPage = 1
          └─ POST getApprovalPage(productCode="PRD001")
               └─ SQL EXISTS(detail.product_code = "PRD001")
                    └─ 返回所有包含该产品的有权限审批单
```

---

## 15. 需求二的最小核心实现（教学简化版）

### 15.1 前端：显示名称，提交编码

```vue
<el-select v-model="searchForm.productCode" @change="search">
  <el-option
    v-for="product in options"
    :label="product.productName"
    :value="product.productCode"
  />
</el-select>
```

```javascript
search() {
  this.page.currentPage = 1
  apiGetApprovalPage({
    ...this.searchForm,       // 这里包含 productCode
    searchType: this.searchType,
    page: this.page.currentPage,
    limit: this.page.pageSize
  })
}
```

### 15.2 后端：动态添加 EXISTS 条件

```xml
<if test="productCode != null and productCode != ''">
  AND EXISTS (
    SELECT 1
    FROM my_frozen_product_quote_detail detail
    WHERE detail.flow_no = t.flow_no
      AND detail.product_code = #{productCode}
      AND detail.status = 1
  )
</if>
```

记忆方式：

```text
名称负责给人看，编码负责机器精确查。
```

---

## 16. `searchType` 权限流程

产品条件只是附加 `AND` 条件，不会替代原权限条件。

### 16.1 待我审批：`searchType = 1`

SQL 要求：

- 当前登录人工号包含在 `t1.current_user_job`；
- 当前节点属于指定运营节点；
- N4 还要求 `t.crt_tag = '4'`。

核心条件：`MyFrozenProductQuoteApplyInfoMapper.xml:285-288`。

### 16.2 全部：`searchType = 2`

Manager 会读取 Nacos 中配置的运营负责人列表：

- 当前人是负责人：把 `dto.jobNo` 清空，从而移除按个人限制的 SQL 块；
- 当前人不是负责人：查询当前由自己处理或自己历史审批过的数据，并排除 N1。

证据：

- Manager 分支：`MyFrozenProductQuoteApplyInfoListManger.java:256-265`
- SQL 分支：`MyFrozenProductQuoteApplyInfoMapper.xml:289-292`

代码第 256 行注释称“工号不在配置中则返回为空”，但实际实现并没有直接返回空，而是按“与我相关”范围查询。学习时应以代码行为为准。

负责人被清空 `jobNo` 后，整个 `searchType=2 and jobNo != ''` SQL 块都不会执行，其中 `current_node != 'N1'` 也不会执行。这是当前实现的精确结果。

### 16.3 PC 与 H5 范围

本需求页面调用 PC 接口：

```text
/apply/list/getApprovalPage
```

同 Controller 还有 H5 专用：

```text
/apply/list/getApprovalPageV2
```

H5 使用 `getApprovalListSqlV2`，当前该 SQL 片段没有 `productCode` 条件。因此不能说“所有审批列表接口都支持产品筛选”；准确范围是当前 PC 报价审批页面。

---

## 17. 需求二的表关系

```text
my_frozen_product_quote_apply_info t       报价申请主表
        │ flow_no
        ├──────────────┐
        ▼              ▼
my_frozen_product_quote_flow t1       my_frozen_product_quote_detail detail
流程和当前处理人                         产品明细（编码、名称、重量）

my_frozen_product_quote_flow_approval_record t2
历史审批记录，通过 apply_no 与主表关联
```

| 表 | 在当前查询中的作用 |
|---|---|
| `my_frozen_product_quote_apply_info` | 审批申请主单、客户、状态、创建时间 |
| `my_frozen_product_quote_flow` | 当前节点、当前处理人、订单类型、流程类型 |
| `my_frozen_product_quote_flow_approval_record` | 查询某用户历史处理过的审批单 |
| `my_frozen_product_quote_detail` | 判断审批单是否包含选中的 `productCode`，并补充全部产品名称和重量 |

产品明细实体证据：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-api/src/main/java/org/muyuan/slaughter/frozen/business/entity/quote/MyFrozenProductQuoteDetailDO.java:26-155`。

本地没有数据库 DDL 和执行计划，因此只能确认代码使用了 `=` 精确条件；不能确认数据库一定存在合适索引，也不能断言优化器一定使用某个 B+ 树索引。

---

## 18. 两个相关接口的设计摘要

### 18.1 产品候选项接口

| 项目 | 内容 |
|---|---|
| 浏览器地址 | `POST /api/muyuan-slaughter-opms/myPreOrderInfo/getProductList` |
| 服务内路径 | `POST /myPreOrderInfo/getProductList` |
| 请求 DTO | `ProductDto { productCode, productName }`；当前页面发送 `{}` |
| 浏览器响应 | `R<List<MyBaseCustomerProduct>>` |
| 页面使用字段 | `productName`、`productCode` |
| 下游 | `POST /api/slaughter_product_center/common/myBaseDataRest/getProductByNameCode` |
| 失败行为 | OPMS Manager 捕获异常后通常返回空列表；前端可能只看到无候选项 |

### 18.2 审批分页接口

| 项目 | 内容 |
|---|---|
| 浏览器地址 | `POST /api/muyuan-slaughter-frozen-business/apply/list/getApprovalPage` |
| 服务内路径 | `POST /apply/list/getApprovalPage` |
| 请求 DTO | `MyFrozenProductQuoteApplyApprovalDTO` |
| 产品参数 | `productCode: String`，可选 |
| 身份参数 | `jobNo` 由 Controller 使用登录账号覆盖 |
| 查询方式 | `EXISTS + detail.product_code = #{productCode}` |
| 返回 | `R<IPage<MyFrozenProductQuoteApplyApprovalVO>>` |
| 页面使用 | `records`、`total` |
| 不传/清空产品 | 不添加 `EXISTS` 产品条件，保持原查询范围 |

---

## 19. 需求二当前实现的边界与注意点

### 19.1 产品数据源不是报价库本地表

当前候选项来自外部产品中心，审批明细来自 `my_frozen_product_quote_detail`。功能成立的前提是两边使用同一套 `productCode` 编码体系。当前工作区没有代码证明这种一致性怎样维护。

### 19.2 产品中心内部无法继续分析

当前缺少 `slaughter_product_center` 源码，所以不能确认：

- 空条件是否一定返回全部产品；
- 用了什么表和 SQL；
- 是否过滤停用产品；
- 排序和去重规则；
- 是否有空名称或重复编码；
- 下游索引和性能。

### 19.3 全量加载的性能取决于产品规模

每个 tab 组件实例挂载都会：

- 请求一份候选产品列表；
- 保存到 `allProductList`；
- 再复制到 `productOptions`；
- 每次输入遍历全量数组。

产品数量较小时简单直观；数量很大时才可能出现响应体、内存和输入过滤成本。当前没有运行数据，不能判断是否已构成实际性能问题。

### 19.4 前端候选项健壮性

当前代码直接调用：

```javascript
item.label.includes(val)
item.value.includes(val)
```

如果外部产品中心返回空名称或空编码，可能产生运行时异常。当前代码默认下游数据完整。

### 19.5 页面处理了并发返回覆盖

分页请求使用时间戳 `fetchFlag`：只有最后一次发出的请求才能更新页面数据。这可以避免用户快速切换筛选条件时，较早请求晚返回并覆盖新结果。

证据：`toApproval.vue:222-232`。

---

# 第三部分：把两个需求放在一起理解

## 20. 两个需求的数据流差异

| 对比项 | 备货进度推送 | 报价产品筛选 |
|---|---|---|
| 触发者 | 工作区外调度器或人工 HTTP 调用 | 前端页面和用户操作 |
| 是否有前端请求主链 | 没有 | 有 |
| 核心模式 | 后端批处理 + 外部消息副作用 | 前后端查询 + 分页返回 |
| 主要数据来源 | MQ 主表快照、日度加工表、订单表 | 外部产品中心、报价申请/流程/明细表 |
| 输出 | 钉钉消息 + 数据库记录 | JSON 分页结果 + 页面列表 |
| 去重/状态 | 里程碑 publish record | 无推送状态；普通筛选查询 |
| 外部依赖 | MDM、RocketMQ、钉钉服务、可能的外部调度平台 | 网关、OPMS 代理、产品中心 |

## 21. 两条总览架构图

### 21.1 备货推送

```text
上游备货系统 ──MQ──> my-sc-slaughter-oms ──> 备货主表
外部 MDM ──HTTP──> my-sc-slaughter-oms ──> 日度加工表/累计字段
外部调度器 ──HTTP──> 进度计算 Manager
                          ├─> quantity 快照表
                          ├─> publish record 去重表
                          └─> 钉钉适配层 ──> 外部钉钉服务 ──> 客户经理
```

### 21.2 报价筛选

```text
                           ┌─> muyuan-slaughter-opms
浏览器 Vue 报价审批页 ─网关─┤      └─> slaughter_product_center
                           │          └─> 产品名称+编码候选项
                           │
                           └─> muyuan-slaughter-frozen-business
                                  └─> 报价申请/流程/审批记录/产品明细
                                      └─> 按 productCode 返回分页审批单
```

---

## 22. 原技术设计文档与当前代码的主要差异

### 22.1 `技术设计_备货进度推送模板优化.md`

已被当前代码实现的内容：

- 增加已加工、已发货、目前可发货；
- 四个 KG 字段 `HALF_UP` 取整；
- `null` KG 显示 0；
- 推送渠道和里程碑主逻辑保持原结构；
- 生产代码核心修改集中在一个 Manager 文件。

需要以当前代码修正的内容：

1. 文档还是“可实施版本”，当前已经落地；
2. 当前真实模板在句号后有换行；
3. 行号已变化；
4. `/frozenOrderWarnMessage/progressRiskWarn` 不是本功能触发接口；
5. 本地无法证明每天 9:00 的调度配置；
6. 节点记录值实际为 1/2/3/4；
7. 只推当前区间节点，不会补发跨过的低节点；
8. 发送失败后仍可能写 publish record；
9. 当前没有可确认的整体事务或并发唯一约束。

### 22.2 `报价审批页新增「产品名称」筛选-技术设计方案V2.md`

已被当前代码实现的内容：

- 前端单选下拉；
- 名称展示、编码提交；
- DTO 使用 `productCode`；
- Mapper 使用 `product_code = #{productCode}`；
- 使用 `EXISTS`；
- count/list 共享条件；
- 仅 PC `getApprovalPage`，没有扩到 H5 V2。

需要以当前代码修正的内容：

1. 文档中的 `productName + LIKE + el-input` 是实施前历史，不是当前现状；
2. 产品候选项真实链经过 `Muyuan-Biz-slaughter-opms` 并调用外部产品中心；
3. 本地 `MyMdProductSpecController/my_md_product_spec` 不是页面真实调用链；
4. “远程搜索”实际是挂载时全量获取、输入时本地过滤；
5. “页面加载一次”准确说是每个 tab 组件实例挂载一次；
6. 当前没有 `handleProductSelect/handleProductClear`，统一由 `@change="search"` 完成；
7. 与参考 `customerBar.vue` 只是思路相近，空输入等细节不完全一致；
8. 没有 DDL/执行计划，不能把“一定使用索引、性能大幅提升”写成已验证事实；
9. 当前仍无法验证产品中心内部、编码一致性机制和生产网关规则。

---

## 23. 推荐的代码阅读顺序

如果你要亲自跟代码，建议按“入口 → 参数 → 业务 → SQL → 返回”阅读，不要一开始就全库搜索。

### 23.1 先读报价筛选（更容易入门）

1. `my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/index.vue:1`
2. `my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/toApproval.vue:1`
3. `my-a-soa-front-slaughter-opms/src/api/slaughter-opms/frozenApproval/index.js:4`
4. `muyuan-slaughter-frozen-business/.../FrozenProductQuoteApplyInfoListController.java:45`
5. `muyuan-slaughter-frozen-business/.../MyFrozenProductQuoteApplyApprovalDTO.java:20`
6. `muyuan-slaughter-frozen-business/.../MyFrozenProductQuoteApplyInfoListManger.java:245`
7. `muyuan-slaughter-frozen-business/.../MyFrozenProductQuoteApplyInfoMapper.xml:284`
8. `muyuan-slaughter-frozen-business/.../MyFrozenProductQuoteDetailMapper.xml:116`
9. 再回头读产品候选项的 `MyPreOrderService.java:99` 和 `MyPreOrderManager.java:228`

### 23.2 再读备货推送（数据来源更多）

1. `MyFrozenOrderStockPreparationTaskService.java:152`
2. `MyFrozenOrderStockPreparationTaskManager.java:1822`
3. `MyFrozenOrderStockPreparationTaskMapper.xml:365`
4. `MyFrozenOrderStockPreparationDailyTaskMapper.xml:144`
5. `MyFrozenOrderStockPreparationPublishRecordMapper.xml:22`
6. 回到 Manager 的 `:1913-2024` 看公式、节点和记录
7. Manager 的 `:2036-2081` 看模板和取整
8. `MySendDingDingMessageBiz.java:40` 看外部发送边界
9. 最后读 MQ Listener 和 `getDailyTaskWork()`，补齐字段来源

---

## 24. 初学者术语表

| 术语 | 通俗解释 |
|---|---|
| DTO | 请求参数对象，负责把 JSON 字段接进 Java |
| VO | 返回给页面或用于查询展示的数据对象 |
| DO / Entity | 与数据库表字段对应的数据对象 |
| Manager | 将多个查询、计算和外部调用串成完整业务流程 |
| Biz | 项目中的数据访问封装，通常继续调用 Mapper |
| Mapper | Java 与 SQL 之间的接口 |
| Mapper XML | MyBatis 动态 SQL 所在文件 |
| `EXISTS` | 只检查关联数据是否至少存在一条，不把明细展开成多行 |
| 动态 SQL `<if>` | 参数有值时才把某段条件拼进 SQL |
| `flowNo` | 将报价申请、流程和产品明细串起来的流程编号 |
| `searchType` | 用于区分待我审批、全部、已审批等数据范围 |
| `mainId` | 备货日度、进度和推送记录关联备货主任务的 ID |
| T-1 / T-3 | 相对今天往前 1 天/3 天的统计截止日期 |
| `HALF_UP` | 常见“四舍五入”规则，1.5 → 2 |
| 网关 | 浏览器与后端服务之间的统一入口，按服务名和路径转发请求 |
| MQ | 消息队列；上游异步把备货任务变更发送给 OMS |
| 幂等/去重 | 同一业务重复触发时，尽量避免产生重复结果 |

---

## 25. 证据边界与分析结论

### 25.1 已由当前本地代码确认

- 两个需求均已经在当前分支代码中实现；
- 备货推送的 HTTP 入口、计算公式、区间节点、消息字段、取整、记录表和钉钉适配调用；
- 报价页两个 tab、产品候选项加载、本地过滤、`productCode` 请求、登录人工号覆盖、Manager 分页流程、SQL `EXISTS`、响应回填；
- 报价筛选的产品候选项真实经过 OPMS 并调用产品中心；
- PC count/list 共用产品条件，H5 V2 SQL 没有该条件。

### 25.2 当前工作区不能确认

- 备货进度生产调度器、cron 和“每天 9:00”的实际配置；
- 上游备货 MQ 生产者的原始字段计算；
- 钉钉服务内部 URL、重试、最终送达确认；
- 产品中心内部 Controller、Mapper、SQL、表、过滤和排序；
- 生产网关完整动态路由及路径重写；
- 数据库真实 DDL、索引、唯一约束、外键和执行计划；
- 生产环境实际调用结果和数据规模。

### 25.3 未执行的验证

为了严格遵守“严禁修改本地代码和远程代码”，本次没有运行可能生成构建物或缓存的 Maven、npm、测试命令，也没有启动服务或访问数据库/远程接口。因此本文是**当前源码实现分析**，不是运行环境验收报告。

---

## 26. 最终总结

可以用两句话记住整个实现：

1. **备货推送**：OMS 从 MQ 和日度加工数据形成备货快照，外部调用计算接口后按当前进度区间决定节点，拼接包含三项新增 KG 信息的钉钉文本，然后记录该节点。
2. **报价筛选**：页面先通过 OPMS 从产品中心取得“名称—编码”候选项，用户看到名称但提交编码，冻品报价服务再用 `EXISTS + product_code =` 找出包含该产品且当前用户有权限查看的审批单，并返回完整分页列表。

两个需求共同体现了公司项目中常见的模式：

```text
前端只负责交互和参数；
Manager 负责编排；
Mapper SQL 负责精确的数据筛选；
外部系统通过网关、HTTP、MQ 或消息服务连接；
分析实现时必须沿真实调用链逐层核验，不能只看设计文档中的预计方案。
```

---

## 附录 B：独立复核证明

- 复核结论：`PASS`
- 问题数量：`BLOCKER 0 / MAJOR 0 / MINOR 0 / UNVERIFIED 0`
- 复核方式：使用未参与正文分析的全新隔离代理，只读重新搜索原始设计材料和当前本地代码；未向审查者提供作者搜索记录或预期结论。
- 复核范围：项目和模块边界；备货 MQ、日加工、累计/调厂、触发、进度、节点、去重、模板和钉钉失败语义；报价候选项跨仓链、前端交互、权限、分页、SQL、回填和 PC/H5 范围；接口示例、证据边界和修改边界。
- 被复核载荷 SHA-256（写入本附录和标题复核状态之前）：`d1501fafb6ec2f099e2962473bef7ccbedac1ae85847e82e3f75d7c08a6e257e`
- 复核闭环：首轮审查发现 2 个 MAJOR、3 个 MINOR，已按代码证据修订 MQ 新增/更新差异、Listener 异常边界、Java 后端仓库数量、调厂重算和工作区状态，并收紧外部请求线格式/候选列表语义；修订后由全新隔离实例重新审查并获得 PASS。
- 失效条件：需求材料变化、相关代码基线变化，或本文除复核证明元数据外的正文被修改时，本 PASS 失效，需重新核验。

---

## 附录 A：核心证据索引

### A.1 备货进度推送

- HTTP 入口：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/service/order/beihuo/MyFrozenOrderStockPreparationTaskService.java:152-163`
- 进度主流程：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/manager/order/beihuo/MyFrozenOrderStockPreparationTaskManager.java:1822-2031`
- 当前消息模板：同文件 `:2036-2071`
- 四舍五入：同文件 `:2073-2081`
- 累计字段计算：同文件 `:1703-1807`
- 外部日加工获取：同文件 `:1126-1238`
- MQ 消费：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-service/src/main/java/com/muyuan/platform/slaughter/oms/order/listener/MyFrozenOrderStockPreparationTaskListener.java:33-96`
- 主任务 SQL：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-dal/src/main/resources/mapper/order/beihuo/MyFrozenOrderStockPreparationTaskMapper.xml:365-371`
- T-1/T-3 SQL：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-dal/src/main/resources/mapper/order/beihuo/MyFrozenOrderStockPreparationDailyTaskMapper.xml:144-153`
- 推送记录 SQL：`my-sc-slaughter-oms/my-sc-slaughter-oms-order-dal/src/main/resources/mapper/order/beihuo/MyFrozenOrderStockPreparationPublishRecordMapper.xml:22-53`
- 钉钉适配：`my-sc-slaughter-oms/my-sc-slaughter-oms-common-dal/src/main/java/com/muyuan/platform/slaughter/oms/common/dal/biz/MySendDingDingMessageBiz.java:40-59`

### A.2 报价审批产品筛选

- tab 入口：`my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/index.vue:1-10`
- 产品控件：`my-a-soa-front-slaughter-opms/src/views/slaughter-opms/orderManage/orderDetail/toApproval.vue:25-37`
- 产品加载/本地过滤：同文件 `:177-213`
- 分页参数/响应：同文件 `:214-232`
- 审批 API：`my-a-soa-front-slaughter-opms/src/api/slaughter-opms/frozenApproval/index.js:4-10`
- 产品 API：`my-a-soa-front-slaughter-opms/src/api/slaughter-opms/frozenManagement/orderStockingProgressSchedule.js:31-38`
- 产品代理 Controller：`Muyuan-Biz-slaughter-opms/muyuan-service/muyuan-slaughter-opms-external-service/src/main/java/org/muyuan/slaughter/opms/external/service/outside/order/MyPreOrderService.java:99-103`
- 产品代理 Manager：`Muyuan-Biz-slaughter-opms/muyuan-service/muyuan-slaughter-opms-oms-service/src/main/java/org/muyuan/slaughter/opms/oms/service/manager/order/beihuo/MyPreOrderManager.java:228-260`
- 产品中心路径：`Muyuan-Biz-slaughter-opms/muyuan-service-api/muyuan-slaughter-opms-api/src/main/java/org/muyuan/slaughter/opms/constant/UrlConstants.java:496-498`
- 审批 DTO：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-api/src/main/java/org/muyuan/slaughter/frozen/business/dto/quote/MyFrozenProductQuoteApplyApprovalDTO.java:20-57`
- 审批 Controller：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-service/src/main/java/org/muyuan/slaughter/frozen/business/controller/quote/FrozenProductQuoteApplyInfoListController.java:45-55`
- 审批 Manager：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-service/src/main/java/org/muyuan/slaughter/frozen/business/manager/quote/MyFrozenProductQuoteApplyInfoListManger.java:245-317`
- 公共筛选 SQL：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-dal/src/main/java/org/muyuan/slaughter/frozen/business/mapper/quote/MyFrozenProductQuoteApplyInfoMapper.xml:284-319`
- count/list：同文件 `:320-328`、`:366-413`
- 产品明细回填 SQL：`muyuan-slaughter-frozen-business/my-slaughter-frozen-order-dal/src/main/java/org/muyuan/slaughter/frozen/business/mapper/quote/MyFrozenProductQuoteDetailMapper.xml:116-126`
